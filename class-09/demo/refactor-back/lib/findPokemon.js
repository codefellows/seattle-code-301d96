'use strict';

// ANTI-pattern don't import files that import "this" file.
// const app = require('../server.js');

// returns a pokemon if found in pokedex / or null
function findPokemon(pokedex, pokemon) {
  // searching pokedex for pokemon
  // if no query is present return all pokemon in pokedex

};


module.exports = findPokemon;

/** Use an object to export multiple things!
 * module.exports = {}
 */
